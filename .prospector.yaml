inherits:
  - strictness_low

uses:
  - django

pylint:
  options:
    max-line-length: 120
  disable:
    - import-outside-toplevel

pep8:
  options:
    max-line-length: 120
  disable:
    - N802

mccabe:
  options:
    max-complexity: 9
